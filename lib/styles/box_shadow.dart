import 'package:flutter/material.dart';

final BoxShadow shadow = BoxShadow(
  color: Colors.black.withOpacity(0.1), // Màu của bóng đổ và độ mờ
  spreadRadius: 0, // Độ rộng của bóng đổ
  blurRadius: 30, // Độ mờ của bóng đổ
  offset: const Offset(5, 5), // Vị trí của bóng đổ
);
